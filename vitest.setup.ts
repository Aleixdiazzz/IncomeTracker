// vitest.setup.ts
// Owner: Tests
// Purpose: Test global setup. Pulls in jest-dom matchers (e.g. `toBeInTheDocument`)
// so component tests read naturally.
import "@testing-library/jest-dom/vitest";
